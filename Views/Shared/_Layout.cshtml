@using Microsoft.AspNetCore.Identity
@using expense_tracker.Models
@inject UserManager<ApplicationUser> UserManager

@{
    // 讀取當前登入者的資料 
    var user = await UserManager.GetUserAsync(User);
    var fullName = user?.FullName ?? "Guest"; // 沒登入顯示 Guest

    // 處理圖片
    var profilePic = "/profile.jpg";
    if (user?.ProfilePicture != null)
    {
        var base64 = Convert.ToBase64String(user.ProfilePicture);
        profilePic = $"data:image/*;base64,{base64}";
    }
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Expense_Tracker</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.syncfusion.com/ej2/20.1.55/material.css" />
    <link href="https://cdn.syncfusion.com/ej2/20.1.55/bootstrap5-dark.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/expense_tracker.styles.css" asp-append-version="true" />
    <script src="https://cdn.syncfusion.com/ej2/20.1.55/dist/ej2.min.js"></script>
</head>
<body class="e-bigger">

    <partial name="_SideBar"></partial>

    <nav class="navbar sticky-top">
        <div class="container-fluid py-2 px-4">
           

            <div class="d-flex align-items-center me-auto">
                <div class="dropdown">
                    <button class="btn btn-success rounded-circle shadow-none" type="button"
                            id="searchDropdown" data-bs-toggle="dropdown" aria-expanded="false"
                            style="width: 40px; height: 40px; padding: 0; display: flex; align-items: center; justify-content: center;">
                        <i class="fa-solid fa-magnifying-glass"></i>
                    </button>

                    <ul class="dropdown-menu dropdown-menu-dark shadow-lg mt-3 p-2 border-secondary"
                        aria-labelledby="searchDropdown"
                        style="background-color: #212b36; border: 1px solid #444; border-radius: 12px; min-width: 220px;">

                        <li class="mb-1">
                            <a class="dropdown-item text-white rounded py-2 d-flex align-items-center"
                               href="/Search/GeneralSearch?searchType=Transaction"
                               onmouseover="this.style.backgroundColor='#34495e';"
                               onmouseout="this.style.backgroundColor='transparent';">
                                <div class="icon-box bg-dark rounded-circle me-3 d-flex align-items-center justify-content-center" style="width:35px; height:35px;">
                                    <i class="fa-solid fa-arrow-right-arrow-left text-warning" style="font-size: 0.9rem;"></i>
                                </div>
                                <span class="fw-bold" style="font-size: 0.95rem;">Search Transactions</span>
                            </a>
                        </li>

                        <li><hr class="dropdown-divider border-secondary my-1"></li>

                        <li>
                            <a class="dropdown-item text-white rounded py-2 d-flex align-items-center"
                               href="/Search/GeneralSearch?searchType=Budget"
                               onmouseover="this.style.backgroundColor='#34495e';"
                               onmouseout="this.style.backgroundColor='transparent';">
                                <div class="icon-box bg-dark rounded-circle me-3 d-flex align-items-center justify-content-center" style="width:35px; height:35px;">
                                    <i class="fa-solid fa-wallet text-info" style="font-size: 0.9rem;"></i>
                                </div>
                                <span class="fw-bold" style="font-size: 0.95rem;">Search Budgets</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>



            <div class="d-flex align-items-center">
                <i class="fa-solid fa-bell me-3 text-muted" style="cursor:pointer;"></i>
                <i class="fa-solid fa-message me-3 text-muted" style="cursor:pointer;"></i>

                <img src="@profilePic"
                     class="profile-pic"
                     style="width: 40px; height: 40px; border-radius: 50%; object-fit: cover; border: 3px solid #fff;" />
            </div>
        </div>
    </nav>

    <div class="main-content">
        <div class="container mt-5 ">
            <div class="row justify-content-center">
                <div class="@(ViewData["isDashboard"] == null ? "col-md-10" : "col-md-12")">
                    <main role="main" class="pb-3">
                        @if (ViewData["isDashboard"] == null)
                        {
                            <div class="row">
                                <div class="col-md-7">
                                    <div class="d-flex flex-column mb-5">
                                        <div class="d-flex mb-1">
                                            <div class="flex-grow-1">
                                                <h3 class="fw-bold">@ViewData["PageTitle"]</h3>
                                            </div>
                                            <div>
                                                @if (ViewData["PageActionText"] != null)
                                                {
                                                    <a class="btn btn-success" href="@ViewData["PageActionUrl"]">
                                                        @ViewData["PageActionText"]
                                                    </a>
                                                }
                                            </div>
                                        </div>
                                        <ejs-breadcrumb id="breadcrumb"
                                                        separatorTemplate="@Html.Raw("<span class='fa-solid fa-angle-right'></span>")"></ejs-breadcrumb>
                                    </div>
                                </div>
                            </div>
                        }
                        @RenderBody()
                    </main>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
    <ejs-scripts></ejs-scripts>
</body>
</html>